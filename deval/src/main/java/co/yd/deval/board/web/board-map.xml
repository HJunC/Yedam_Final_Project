<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.yd.deval.board.web.board-map">


	<select resultType="co.yd.deval.board.web.BoardVO"
		id="boardSelectList ">
		SELECT *FROM BOARD
	</select>


	<select resultType="co.yd.deval.board.web.BoardVO"
		id="boardSelect" parameterType="co.yd.deval.board.web.BoardVO">
		SELECT *FROM BOARD
		WHERE ID = #{id}
	</select>

	<insert id="boardInsert"
		parameterType="co.yd.deval.board.web.BoardVO">
		INSERT INTO BOARD (ID, PASSWORD, NAME) VALUES (#{id},
		#{password}, #{name})
	</insert>

	<delete id="boardDelete" parameterType="String">
		DELETE FROM BOARD
		WHERE ID = #{id}
	</delete>

	<update id="boardUpdate"
		parameterType="co.yd.deval.board.web.BoardVO">
		UPDATE BOARD
		<set>
			<if test="password != null">PASSWORD = #{password},</if>
			<if test="name != null">NAME = #{name},</if>
		</set>
		WHERE ID = #{id}
	</update>

</mapper>

