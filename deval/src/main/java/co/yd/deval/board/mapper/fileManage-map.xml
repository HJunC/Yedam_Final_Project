<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.yd.deval.board.mapper.photoMapper">
<insert  id="fileManageInsert"  parameterType="co.yd.deval.board.service.FileManageVO">INSERT INTO FILEMANAGE
		(FILE_NO,
		NAME,
		TYPE,
		BOARD_NO
		)

VALUES 
		#{file_no},
		#{name},
		#{type},
		#{board_no}
		) 
		
</insert>

</mapper>